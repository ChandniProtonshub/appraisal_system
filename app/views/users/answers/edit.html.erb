<%= form_for @answer, url: users_answer_path(@question, @answer), method: :put do |form| %>

<h3> Edit Answer </h3>
 
 <div class="field">
 <%= label_tag :answer, "Answer:" %><br>
 <%= text_area_tag "questions[#{question.id}][description]" %><br>
</div>

<div class="actions">
    <%= form.submit "Update" %>
</div>

<% end %>
